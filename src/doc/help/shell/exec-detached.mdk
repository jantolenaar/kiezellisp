.index shell:exec-detached
.usage builtin-function
.syntax
(shell:exec-detached prog &rest args)

.description
Executes the server or gui program `prog` with arguments `args`.

.see-also
.api
shell:run shell:exec
