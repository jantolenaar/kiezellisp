.index reduced
.usage builtin-function
.syntax
(reduced value)

.description
A reducer function may wrap a `value` in a `ReduceBreakValue` object
to signal the `reduce` or `transduce` function to stop the iteration and 
return the wrapped value instead.

.see-also
.api
reducedp unreduced



