.index compare-ci
.usage builtin-function
.syntax
(compare-ci a1 a2)

.description
Like `compare`, but compares strings and characters case-insensitively.

.examples
(compare "aap" "Aap")
(compare-ci "aap" "Aap")

